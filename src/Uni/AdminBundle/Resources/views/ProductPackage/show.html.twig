{% extends 'UniAdminBundle:Base:layout.html.twig' %}
{% use 'UniAdminBundle:Base:icons.html.twig' %}
{% use 'UniAdminBundle:Base:flashbag.html.twig' %}

{% trans_default_domain 'UniAdminBundle' %}

{% block title %}{{ 'productpackage.show.title'|trans({'%id%':productPackage.id}) }}{% endblock %}

{% block admin %}
    <div id="page-wrapper">
        <div class="row" id="page-header">
            <div class="col-lg-12">
                <h1 class="page-header">{% block icons %}{{ parent() }}{% endblock %}{{ 'productpackage.show.title'|trans({'%id%':productPackage.id}) }}</h1>
                {% block flashbag %}{{ parent() }}{% endblock %}
            </div>
        </div>
        <!-- /#page-header -->

        <div class="row" id="page-toolbar">
            <div class="col-lg-12">
                <div class="btn-toolbar">
                    <a type="button" class="btn btn-default" href="{{ path('admin_productpackage_index') }}">{{ icon('list fa-fw') }}&nbsp;{{ 'productpackage.index.link'|trans }}</a>
                    <a type="button" class="btn btn-primary" href="{{ path('admin_productpackage_edit', { 'id': productPackage.id }) }}">{{ icon('edit fa-fw') }}&nbsp;{{ 'productpackage.edit.link'|trans }}</a>
                    <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#modalDelete">{{ icon('trash-o fa-fw') }}&nbsp;{{ 'productpackage.delete.link'|trans }}</button>
                </div>
            </div>
        </div>
        <!-- /#page-toolbar -->

        <div class="row" id="page-show">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h2 class="panel-title">{{ 'productpackage.show.data'|trans }}</h2>
                    </div>
                    <div class="panel-body">
                        <dl class="dl-horizontal">
                            <dt>{{ 'productpackage.form.id'|trans }}</dt>
                                <dd>{% if productPackage.id %}{{ productPackage.id }}{% endif %}</dd>
                            <dt>{{ 'productpackage.form.height'|trans }}</dt>
                                <dd>{% if productPackage.height %}{{ productPackage.height }}{% endif %}</dd>
                            <dt>{{ 'productpackage.form.width'|trans }}</dt>
                                <dd>{% if productPackage.width %}{{ productPackage.width }}{% endif %}</dd>
                            <dt>{{ 'productpackage.form.length'|trans }}</dt>
                                <dd>{% if productPackage.length %}{{ productPackage.length }}{% endif %}</dd>
                            <dt>{{ 'productpackage.form.weight'|trans }}</dt>
                                <dd>{% if productPackage.weight %}{{ productPackage.weight }}{% endif %}</dd>
                            <dt>{{ 'productpackage.form.created_at'|trans }}</dt>
                                <dd>{% if productPackage.createdat %}{{ productPackage.createdat|localizeddate('full', 'full', 'es_ES', 'America/Santiago', "d 'de' LLLL 'de' y - HH:mm") }}&nbsp;({{ productPackage.createdat|ago }}){% endif %}</dd>
                            <dt>{{ 'productpackage.form.updated_at'|trans }}</dt>
                                <dd>{% if productPackage.updatedat %}{{ productPackage.updatedat|localizeddate('full', 'full', 'es_ES', 'America/Santiago', "d 'de' LLLL 'de' y - HH:mm") }}&nbsp;({{ productPackage.updatedat|ago }}){% endif %}</dd>                        </dl>
                    </div>
                </div>
            </div>
        </div>
        <!-- /#page-show -->

        <div id="page-modal">
            <div class="modal fade" id="modalDelete" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                <div class="modal-dialog modal-md" role="document">
                    <div class="modal-content">
                        <div class="modal-header">
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close">{{ icon('times') }}</button>
                            <h4 class="modal-title" id="myModalLabel">{{ 'productpackage.delete.title'|trans }}</h4>
                        </div>
                        <div class="modal-body">
                            <div class="row">
                                <div class="col-xs-2 text-center">{{ icon('exclamation-triangle fa-5x') }}</div>
                                <div class="col-xs-10"><p>{{ 'productpackage.delete.message'|trans }}</p></div>
                            </div>
                        </div>
                        <div class="modal-footer">
                            {{ form_start(deleteForm) }}
                                <button type="submit" class="btn btn-danger pull-right">{{ icon('trash-o fa-fw') }}&nbsp;{{ 'productpackage.delete.submit'|trans }}</button>
                            {{ form_end(deleteForm) }}
                        </div>
                    </div>
                </div>
            </div>

        </div>
        <!-- /#page-modal -->

    </div>
    <!-- /#page-wrapper -->
{% endblock %}

