{% extends 'UniPortfolioBundle:Base:layout.html.twig' %}

{% trans_default_domain 'UniPortfolioBundle' %}

{% block title %}&nbsp;-&nbsp;{{ 'portfolioitem.show.title'|trans({'%id%':portfolioitem.id}) }}{% endblock %}

{% block portfolio %}
    <div id="page-wrapper">
        <div class="row" id="page-header">
            <div class="col-lg-12">
                <h1 class="page-header">{% block icons %}{{ parent() }}{% endblock %}{{ 'portfolioitem.show.title'|trans({'%id%':portfolioitem.id}) }}</h1>
                {% block flashbag %}{{ parent() }}{% endblock %}
            </div>
        </div>
        <!-- /#page-header -->

        <div class="row" id="page-toolbar">
            <div class="col-lg-12">
                <div class="btn-toolbar">
                    <a type="button" class="btn btn-default" href="{{ path('portfolio_portfolioitem_index') }}">{{ icon('list fa-fw') }}&nbsp;{{ 'portfolioitem.index.link'|trans }}</a>
                    <a type="button" class="btn btn-primary" href="{{ path('portfolio_portfolioitem_edit', { 'id': portfolioitem.id }) }}">{{ icon('edit fa-fw') }}&nbsp;{{ 'portfolioitem.edit.link'|trans }}</a>
                    <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#modalDelete">{{ icon('trash-o fa-fw') }}&nbsp;{{ 'portfolioitem.delete.link'|trans }}</button>
                </div>
            </div>
        </div>
        <!-- /#page-toolbar -->

        <div class="row" id="page-show">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h2 class="panel-title">{{ 'portfolioitem.show.data'|trans }}</h2>
                    </div>
                    <div class="panel-body">
                        <dl class="dl-horizontal">
                            <dt>{{ 'portfolioitem.form.id'|trans }}</dt>
                                <dd>{% if portfolioitem.id %}{{ portfolioitem.id }}{% endif %}</dd>
                            <dt>{{ 'portfolioitem.form.portfoliocategory'|trans }}</dt>
                                <dd>{% if portfolioitem.portfoliocategory %}{{ portfolioitem.portfoliocategory }}{% endif %}</dd>
                            <dt>{{ 'portfolioitem.form.document'|trans }}</dt>
                                <dd>{% if portfolioitem.document %}{{ portfolioitem.document }}{% endif %}</dd>
                            <dt>{{ 'portfolioitem.form.created_at'|trans }}</dt>
                                <dd>{% if portfolioitem.createdat %}{{ portfolioitem.createdat|localizeddate('full', 'full', 'es_ES', 'America/Santiago', "d 'de' LLLL 'de' y - HH:mm") }}&nbsp;({{ portfolioitem.createdat|ago }}){% endif %}</dd>
                            <dt>{{ 'portfolioitem.form.updated_at'|trans }}</dt>
                                <dd>{% if portfolioitem.updatedat %}{{ portfolioitem.updatedat|localizeddate('full', 'full', 'es_ES', 'America/Santiago', "d 'de' LLLL 'de' y - HH:mm") }}&nbsp;({{ portfolioitem.updatedat|ago }}){% endif %}</dd>                        </dl>
                    </div>
                </div>
            </div>
        </div>
        <!-- /#page-show -->

        <div id="page-modal">
            <div class="modal fade" id="modalDelete" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                <div class="modal-dialog modal-md" role="document">
                    <div class="modal-content">
                        <div class="modal-header">
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close">{{ icon('times') }}</button>
                            <h4 class="modal-title" id="myModalLabel">{{ 'portfolioitem.delete.title'|trans }}</h4>
                        </div>
                        <div class="modal-body">
                            <div class="row">
                                <div class="col-xs-2 text-center">{{ icon('exclamation-triangle fa-5x') }}</div>
                                <div class="col-xs-10"><p>{{ 'portfolioitem.delete.message'|trans }}</p></div>
                            </div>
                        </div>
                        <div class="modal-footer">
                            {{ form_start(deleteForm) }}
                                <button type="submit" class="btn btn-danger pull-right">{{ icon('trash-o fa-fw') }}&nbsp;{{ 'portfolioitem.delete.submit'|trans }}</button>
                            {{ form_end(deleteForm) }}
                        </div>
                    </div>
                </div>
            </div>

        </div>
        <!-- /#page-modal -->

    </div>
    <!-- /#page-wrapper -->
{% endblock %}

